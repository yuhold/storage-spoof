## StorageSpoof 1.2.0

### 主要更新

- 主界面重构为“主页 / 应用 / 设置”三页底部导航。
- 主页新增模块连接状态、应用配置统计、工作方式、安全边界和快捷操作。
- 自定义界面与 Material 3 采用两套独立布局与组件风格。
- 应用卡片、设置卡片、底部导航和分组标签支持莫奈动态取色或自定义种子色。
- 权限检查页与关于页完整适配双界面、明暗模式、动态主题色和自定义背景图。
- 应用页新增：
  - 全部 / 只看已修改 / 只看未修改
  - 显示系统应用
  - 默认顺序 / 名称 A–Z
  - 所有排序方式均保持已修改应用优先
- 优化背景图 `CENTER_CROP` 自适应、明亮度调节与最低可读性遮罩。
- 优化圆角、应用容量摘要、大字体显示和可滚动筛选面板。

### 模块兼容性

- Android 16 / targetSdk 36
- libxposed API 102
- AOSP 系统设置 `com.android.settings`
- 小米澎湃 OS 3 安全中心 `com.miui.securitycenter`
- 未配置或已停用的应用仍保持真实统计

### 安全边界

本模块只修改目标宿主读取到的存储统计显示，不修改真实文件、真实磁盘占用、真实可用空间或文件系统。澎湃 OS 顶部存储摘要修正仍限制在对应存储页刷新调用期间。

### 下载说明

- 推荐下载 `StorageSpoof-1.2.0-universal.apk`。
- `arm64-v8a` 为相同签名的兼容命名副本；项目不含原生 `.so`，因此两个 APK 内容一致。
- 请使用 `StorageSpoof-1.2.0-checksums.txt` 校验 SHA-256。
- 免费提供，禁止倒卖、付费下载、商业打包和收费安装。
